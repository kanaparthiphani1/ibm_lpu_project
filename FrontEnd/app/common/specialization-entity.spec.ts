import { SpecializationEntity } from './specialization-entity';

describe('SpecializationEntity', () => {
  it('should create an instance', () => {
    expect(new SpecializationEntity()).toBeTruthy();
  });
});
